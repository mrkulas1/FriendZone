`database_scripts` folder holds files with all the statements used to setup the MySQL database:
    - All CREATE TABLE statements 
    - INSERT statements to fill in sample data
    - Any procedures created

